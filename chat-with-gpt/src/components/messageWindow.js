import styled from "styled-components";

const Wrapper = styled.div`
  height: 100%;
  border: 1px solid blue;
`;

const messageWindow = () => {
  return <Wrapper>messageWindow</Wrapper>;
};

export default messageWindow;
